"""Domain layer package.

The ``domain`` layer contains pure value objects, enums, and entities that
model the structural engineering concepts at the heart of the application.

Import rules
------------
- Must have zero imports from any other project layer.
- Standard-library imports are permitted.
- Third-party imports should be avoided; if unavoidable, document the reason.
"""

